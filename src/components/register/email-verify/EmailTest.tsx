import styled from 'styled-components';

const EmailTab = () => {
  return (
    <EmailTabStyle>
      <h1>EmailTab</h1>
    </EmailTabStyle>
  );
};

const EmailTabStyle = styled.div``;

export default EmailTab;

import styled from 'styled-components';

const FloatingBar = () => {
  return (
    <FloatingBarStyle>
      <h1>FloatingBar</h1>
    </FloatingBarStyle>
  );
};

const FloatingBarStyle = styled.div``;

export default FloatingBar;
